{
  "name": "neonoid",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Arkanoid-like brick breaker with XP progression, super strikes, level editor and local PvP",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
